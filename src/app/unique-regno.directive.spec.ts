import { UniqueRegnoDirective } from './unique-regno.directive';

describe('UniqueRegnoDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueRegnoDirective();
    expect(directive).toBeTruthy();
  });
});
